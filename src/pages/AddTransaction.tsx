import { FunctionComponent } from "react";

interface Props {}

const AddTransaction: FunctionComponent<Props> = (props: Props) => {
  return (
    <>
      <h1>Add a New Transaction</h1>
    </>
  );
};

export default AddTransaction;
